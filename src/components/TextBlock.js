import React from 'react';

export default function TextBlock() {
  return(
    <div className='textblock_container'>
      <h1 className='textblock_title title--h1 text--white'><span className='title--underline'>Heading</span> One</h1>
      <p className='textblock_text text--paragraph text--white'>Remove the duplicates Remove the duplicates Remove the duplicates Remove the duplicates Remove the duplicates Remove the duplicates Remove the duplicates</p>
    </div>
  )
}