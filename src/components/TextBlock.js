import React from 'react';

export default function TextBlock() {
  return(
    <div className='textblock-container'>
      <h1 className='textblock-title title--h1 text--white'><span className='title--underline'>Heading</span> One</h1>
      <h3 className='textblock-title title--h3 text--white'>Remove the duplicates Remove the duplicates Remove the duplicates Remove the duplicates Remove the duplicates Remove the duplicates Remove the duplicates</h3>
    </div>
  )
}